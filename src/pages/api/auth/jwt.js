// import jwt from 'jsonwebtoken';

// // JWTを扱うための関数
// export const signToken = (payload) => {
//     return jwt.sign(payload, SECRET_KEY, { expireIn: '1h'});
// };

// export const verifyToken = (token) => {
//     try {
//         return jwt.verify(token, SECRET_KEY);
//     } catch (error) {
//         return null;
//     }
// };